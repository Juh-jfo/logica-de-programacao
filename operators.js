const sun = 2 + 4;
const diff = 10 - 5;
const multiply = 20 * 2356;
const division = 40 / 5;
const module = 24 % 5;
const power = Math.pow (5, 2);
const squareRoot = Math.sqrt (25);

console.log (sun, diff, multiply, division, module, power, squareRoot);
